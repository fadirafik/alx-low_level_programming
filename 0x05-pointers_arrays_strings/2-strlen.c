#include <stdio.h>
#include <string.h>

/**
 * _strlen - resets a given number to 98
 * @s: is the user input
 *
 * Return: string length
*/

int _strlen(char *s)
{
    int i;

    i = strlen(s);
    return (i);
}
