#include "main.h"
#include <ctype.h>
#include <stdio.h>

/**
 * _abs - check the code.
 * Return:Always 0 if otherwise
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
